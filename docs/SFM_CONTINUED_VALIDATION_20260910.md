# 鱼眼 SfM 继续验证与 COLMAP 对照（2026-09-10）

后续弱连接恢复修改及最终版本冷启动结果见 [2026-09-11 恢复验证](SFM_RECOVERY_VALIDATION_20260911.md)。下文保留本轮历史结果。

本轮在工作区已有 v9 改动之上修复共享内参更新后的候选姿态评分，并重新运行真实图像实验。没有向 Photara 输入 COLMAP 的焦距、畸变、姿态、特征或三维点。保留此前工作区的其他修改。

## 算法修改

`camera_selection.cpp::refine_calibration` 的联合 BA 只纳入成功且非平面退化的图像对，但更新的内参由全部图像对共享。此前其余图像对仍以旧内参下估计的姿态参与新内参评分，可能压低正确标定候选的验证得分。

现在增加一次新内参下的训练匹配姿态重估，比较该候选与 BA 候选的验证得分，保留较优者。原始匹配每三点取一点作为验证的规则不变，验证点不进入 BA 或姿态重估。未修改模型选择阈值、注册门槛、弱连接过滤或 COLMAP 参考。

## 固定相机集合的局部对照

输入为已有 `alameda_tail64/images`，DSC06988–DSC07051 共 64 张原始鱼眼照片；参考是已保存的、独立运行的 COLMAP OPENCV_FISHEYE incremental mapper 64/64 结果。

三次 Photara 均注册 38/64，但具体相机集合不同。下表仅对三次共同注册的 **29 张固定相机**进行一次正尺度、无反射 Sim(3)；没有按误差剔除相机。这是固定集合几何诊断，不替代全部 64 张的注册验收。

| 版本 | 固定公共相机 | 位置 P95 / 最大（参考半径 %） | 旋转 P95 / 最大（°） |
|---|---:|---:|---:|
| 修改前 | 29 | 1.172 / 1.700 | 0.622 / 0.736 |
| 修改后 | 29 | 0.168 / 0.192 | 0.082 / 0.110 |
| 修改后独立重复 | 29 | 0.135 / 0.170 | 0.077 / 0.086 |

![固定相机集合对照](../artifacts/sfm_validation_20260910/tail_fixed_cohort.png)

分别对各次全部 38 张注册相机评估，位置 P95 为 40.863% / 0.529% / 0.322%，旋转 P95 为 2.125° / 0.212° / 0.116°。这些集合不同，不能把其降幅当作严格同组改进。三次整体验收均因注册及参考覆盖不足失败，没有把固定集合的通过冒充完整数据通过。

首次局部运行自动标定初值从 723.204 px 改为 618.009 px。这是原始匹配独立估计结果，不是导入参考焦距。冷缓存重复运行存在匹配/重建差异，不能宣称逐位确定性。

## 回归与全量

所有本轮 Photara 运行均为新输出名和新缓存，参数 `--camera-model auto --mode global --max-features 6000 --window 6`。全部同名已注册相机参与各自对照的 Sim(3)。位置百分比为相机中心差异除以参考相机中心 RMS 半径，不能换算为厘米；不同子集半径不同，也不能横向当作同一绝对误差。

| 数据 | 注册 | 位置 P95（参考半径 %） | 旋转 P95（°） | 自身观测 RMS（px） | 工程门槛 |
|---|---:|---:|---:|---:|---|
| Alameda 前 64 | 64/64 | 0.268 | 0.143 | 0.626 | 通过 |
| Alameda 独立 128 | 128/128 | 0.121 | 0.060 | 0.606 | 通过 |
| Chuan 针孔控制 | 109/109 | 0.494 | 0.174 | 0.558 | 通过 |
| Alameda 全量 | 1735/1742 | 0.219 | 0.177 | 0.713 | 通过 |

全量与旧参考公共相机 1728/1734，注册相机集合与 v9 相同。位置最大差异为 **1.902%**（DSC07008），旋转最大差异为 **3.487°**（DSC08184）；未注册仍为 DSC07015–DSC07020 和 DSC08185。7 张额外注册相机没有旧参考位姿，不能声称其精度已获验证。耗时 484.24 s，峰值工作集 5323 MB，无缓存命中。

全量 v9 的位置 P95 / 最大为 0.201% / 0.850%，本轮为 0.219% / 1.902%；旋转 P95 从 0.164° 到 0.177°。**这是局部子集改善、全量局部尾部变差的结果，不是全面优于 v9。** 修复仍保留，因为它纠正了候选评分流程，并在困难子集固定相机集合及重复运行中验证了收益；全量弱连接段的漂移必须继续单独处理。仅凭工程门槛通过不能宣称问题解决。

![全量对照](../artifacts/sfm_validation_20260910/full_refit.png)

前 64 的位置 P95 较 v9 的 0.249% 略增；独立 128 基本持平，针孔控制略有改善。不声称所有数据所有指标都改善。全量与小数据/测试曾并行运行，耗时仅为运行记录，不作等资源速度基准。重投影是各系统自身观测的自洽指标，观测集合不同，不能直接判定物理精度。

## COLMAP 参考本身的差异

前 64 和独立 128 使用此前新跑的原始鱼眼 COLMAP 子集参考；Chuan 使用已有模型。全量主要对照仍为旧去畸变序列的 1734 张参考，名字映射与来源见 2026-09-09 报告。

额外核查了已完成的原始鱼眼全量 COLMAP `global_mapper` 模型（1742 张）。它与旧全量参考在全部 1734 张公共相机上的位置 P95 差异为 83.140%、最大 2048.738%，最大离群为 DSC07958；旋转 P95 为 2.304°。不删除离群点、不以它替换主要参考，也不把它称作可靠真值。该模型匹配候选含 Photara 检索的图像对名称及更宽序列邻域，特征、匹配和重建由 COLMAP 独立计算；它不是完全独立的候选对检索实验。

困难局部 COLMAP 64 张与旧全量参考也不完全一致：全部 64 张的一次 Sim(3) 后位置 P95 为 35.012%、旋转 P95 为 7.592°。在首次修改后保留的 38 张上单独诊断，两参考位置 P95 为 0.836%、旋转 P95 为 0.698°；这说明局部不同连接部分之间仍有明显争议，不能据某一个参考断言全部相机的物理精度。

## 验证与证据

- CLI、Editor 和 two_view 测试目标 Release 编译成功。
- 鱼眼投影、相对/绝对姿态、自动模型、焦距/畸变等 two_view 测试通过；新增混合平面/非平面、带畸变的合成测试验证未见光线投影误差小于 2 px。
- SfM 验收脚本 6 项测试通过；`git diff --check` 通过。
- `artifacts/sfm_validation_20260910/` 保存运行命令、退出码、冷缓存状态、ASFM、日志、逐相机 CSV、报告 JSON、固定集合对比及图。
- `compare_common.py` / `tail_common_comparison.json` 保存三次实际注册名单和固定 29 张对照；`analyze.py` / `reference_audit.json` 保存参考交叉检查；`provenance.json` 保存源码、可执行文件和参考位姿文件 SHA256。

复现局部实验（输出名必须新建）：

```powershell
python experiments/run_sfm_acceptance.py --exe build/photara/Release/photara.exe --images artifacts/sfm_product_validation_20260909/alameda_tail64/images --reference artifacts/sfm_product_validation_20260909/colmap_tail64/sparse/0 --output-dir artifacts/sfm_validation_20260910 --name tail64_new --timeout-seconds 600 -- --camera-model auto --mode global --max-features 6000 --window 6
```

后续重点是弱连接相机的独立深度与跨段约束，以及参考模型之间的局部几何争议。本轮没有解决困难子集的 26 张未注册问题。
